<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/bootstrap.css">
    <script src="/applicationsDetails.js" defer></script>
    <title>creating new post</title>
    <style>
        body {
            margin-top: 20px;
            color: #1a202c;
            text-align: left;
            background-color: #e2e8f0;
        }

        .main-body {
            padding: 15px;
        }

        .card {
            box-shadow: 0 1px 3px 0 rgba(0, 0, 0, .1), 0 1px 2px 0 rgba(0, 0, 0, .06);
        }

        .card {
            position: relative;
            display: flex;
            flex-direction: column;
            min-width: 0;
            word-wrap: break-word;
            background-color: #fff;
            background-clip: border-box;
            border: 0 solid rgba(0, 0, 0, .125);
            border-radius: .25rem;
        }

        .card-body {
            flex: 1 1 auto;
            min-height: 1px;
            padding: 1rem;
        }

        .gutters-sm {
            margin-right: -8px;
            margin-left: -8px;
        }

        .gutters-sm>.col,
        .gutters-sm>[class*=col-] {
            padding-right: 8px;
            padding-left: 8px;
        }

        .mb-3,
        .my-3 {
            margin-bottom: 1rem !important;
        }

        .bg-gray-300 {
            background-color: #e2e8f0;
        }

        .h-100 {
            height: 100% !important;
        }

        .shadow-none {
            box-shadow: none !important;
        }

        textarea {
            border: none;
            outline: none;
            resize: none;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="main-body">
            <nav aria-label="breadcrumb" class="main-breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/home">home</a></li>
                    <li class="breadcrumb-item"><a href="/profile">profile</a></li>
                </ol>
            </nav>
            <div class="gutters-sm">
                <% if (content.length>0) { %>
                    <% content.forEach(element=> { %>
                        <div class="card my-3" data-oidFF="<%= element.ID %>">
                            <div class="card-body">
                                <h4>user name : <span class="text-info">
                                        <%= element.userName %>
                                    </span>
                                </h4>
                                user email : <a href="mailto:<%= element.userEmail %>">
                                    <%= element.userEmail %>
                                </a>
                                <br>
                                user main job : <span class="text-info">
                                    <%= element.userJob %>
                                </span>
                                <br>
                                application statue : <span id="statue"
                                    class="fw-bold text-<%= element.statue =='rejected'? 'danger':element.statue=='bending'?'warning':'success' %>">
                                    <%= element.statue %>
                                </span>
                                <br><br>
                                <button id="accept" class="btn btn-dark btn-outline-success">accept</button>
                                <button id="reject" class="btn btn-dark btn-outline-danger">reject</button>
                                <button id="clear" class="btn btn-dark btn-outline-warning">reset</button>
                            </div>
                        </div>
                        <% }) %>
                            <% }else{ %>

                                <% } %>
            </div>
        </div>
    </div>
</body>

</html>